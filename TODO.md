# TODO

- [ ] Set up Read the Docs auth (`READTHEDOCS_TOKEN`, or `token` on the provider block).
      Create a token at https://app.readthedocs.org/accounts/tokens/ then export it (do not commit).
- [ ] `tofu apply` [`examples/github-utils`](examples/github-utils) to import the github-utils project and trigger a docs build.
