# TODO

- [x] Set up Read the Docs auth (`READTHEDOCS_TOKEN` from `.envrc.dc` `secrets.readthedocs.token`).
- [x] `tofu apply` [`examples/github-utils`](examples/github-utils) — project `github-utils`.
      Default branch `develop` (has `.readthedocs.yaml`; `mono-repo-dev` does not).
      Successful build `34319731` (commit `fd1340e7b69e`).
      Docs: https://github-utils.readthedocs.io/en/latest/
- [ ] Optional: `infisical-populate-secrets --section=readthedocs` (Infisical path still empty).
